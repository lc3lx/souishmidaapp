// const express = require("express");
// const { protect } = require("../controllers/auth.Controller");
// const {
//   createBroadcast,
//   // getBroadcasts,
//   getBroadcast,
//   updateBroadcast,
//   deleteBroadcast,
//   getBroadcastAnalytics,
// } = require("../controllers/broadcast.controller");

// const router = express.Router();

// // Protect all routes
// router.use(protect);

// // Get all broadcasts
// //router.get("/", getBroadcasts);

// // Create new broadcast
// router.post("/", createBroadcast);

// // Get single broadcast
// router.get("/:id", getBroadcast);

// // Update broadcast
// router.patch("/:id", updateBroadcast);

// // Delete broadcast
// router.delete("/:id", deleteBroadcast);

// // Get broadcast analytics
// router.get("/:id/analytics", getBroadcastAnalytics);

// module.exports = router;
